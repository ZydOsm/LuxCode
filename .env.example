# Copy this file to ".env" (same folder as the app) and fill in the key(s) you need.
# Only GEMINI_API_KEY is required for the default model.

GEMINI_API_KEY=

# Optional, only needed if you pick a gpt-* or claude-* model instead.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
